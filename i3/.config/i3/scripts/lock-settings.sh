#!/usr/bin/env bash

# Configure auto lock behavior
# WARNING_SECONDS is how many seconds the warning will appear before locking
AUTO_LOCK_SECONDS=600
WARNING_SECONDS=30
WARNING_MESSAGE='System will lock in {seconds} seconds'