These repo is mostly everything I need to build an i3 system how I like it.

# Dependencies
Add these here later

# Startup
Run `./my_os/.mega_setup.sh` and the script will take care of the rest

# Battery Health
On startup a battery health notificaiton will be displayed. It logs the battery health to `~/.local/state/battery-health/history.tsv`.
You can also get the current health manually by running `~/.local/bin/notify-battery-health`.