[config]
# Keep the internal panel in the hardware fingerprint while its lid is closed.
# Docked profiles already turn eDP-1 off, so lid events do not need to switch
# profiles or reapply the unchanged external-display mode.
ignore-lid=true
