#!/bin/bash

# Re-read output assignments without serializing transient windows into i3's
# restart state. A full restart here can leave dead authentication dialogs as
# black placeholder windows when autorandr runs during lock/unlock.
i3-msg reload
